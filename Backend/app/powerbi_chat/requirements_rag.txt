# RAG Pipeline Dependencies for Power BI Chat
# Install these dependencies for the RAG functionality

# Core RAG dependencies - using compatible LangChain versions
langchain-core>=0.3.0,<1.0.0
langchain-community>=0.3.0,<1.0.0
langchain-google-genai>=2.1.0,<3.0.0
langchain-postgres>=0.0.15,<1.0.0

# Note: langchain_postgres replaces the deprecated langchain_community.vectorstores.pgvector

# Vector database support
pgvector>=0.2.5,<1.0.0
psycopg[binary]>=3.2.0,<4.0.0

# PBIXRay dependencies for PBIX file parsing
kaitaistruct
apsw
icecream
pandas
xpress9

# Additional utilities
python-dotenv>=1.0.0,<2.0.0
